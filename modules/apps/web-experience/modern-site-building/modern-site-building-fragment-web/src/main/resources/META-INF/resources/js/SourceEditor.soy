{namespace SourceEditor}

/**
 * Renders a SourceEditor
 * @param syntax
 * @param? toolbarItems
 */
{template .render}
	<div class="source-editor" ref="wrapper">
		{call SourceEditorToolbar.render}
			{param ref: 'toolbar' /}
			{param syntax: $syntax /}
			{param items: $toolbarItems /}
		{/call}
		<div ref="editorWrapper" class="source-editor__editor"></div>
	</div>
{/template}